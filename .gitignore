/target
**/*.rs.bk
*.pdb

# NVRTC caches compiled kernels here; regenerable, never commit.
/.nvrtc-cache

# Large generated fixtures. Small committed fixtures live in testdata/ and are
# tracked deliberately — see CLAUDE.md.
/testdata/generated

# Left behind when the remote job scripts are imported for local testing.
__pycache__/

# Engineering notes for Claude Code: design rationale, measurements and the
# process lessons behind them. Kept local for now — it is written for whoever
# is building this, not for whoever is adopting it. README.md is the public
# account of the same project.
CLAUDE.md
